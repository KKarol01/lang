
func comp(a, b) {
	if a < b: {
		return -1;
	} else if a > b: {
		return 1;
	} else {
		for a=10 b = 15; a > -1; ++b: {
			if b > 7: { return b; }
		}
		return a; 
	}
}

a = comp(1, 2);
b = comp(2, 2);
c = comp(3, 2);
