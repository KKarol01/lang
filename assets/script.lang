
func comp(a, b) {
	if a < b: {
		return -1;
	} else if a > b: {
		return 1;
	} else {
		c = 4;
		for a=0 b=3; a < b; ++a: {
			c = c;	
		}
		return a + b * c;
	}
}

a = comp(1, 2);
b = comp(2, 2);
c = comp(3, 2);
